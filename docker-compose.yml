version: '3.9'
services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: "password"
      MYSQL_DATABASE: "mydb"
    volumes:
      - ./db/setup:/docker-entrypoint-initdb.d
    ports:
      - 8082:3306